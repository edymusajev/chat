{
  "name": "chat",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "npx tsc && bun --hot run src/server.ts",
    "start": "bun run src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
